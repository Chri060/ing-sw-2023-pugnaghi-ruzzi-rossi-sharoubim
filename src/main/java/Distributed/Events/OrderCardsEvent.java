package Distributed.Events;

import View.View;

/**
 * Class that implements the event of changing of card order
 */
public class OrderCardsEvent implements Event {

    /**
     * Update method override to change the View when the order of the cards is changed
     *
     * @param view is the View to update
     */
    @Override
    public void update(View view) {
    }
}